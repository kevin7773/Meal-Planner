# Recipe Photos

Optional recipe photos use the recipe ID as the filename:

```text
FDP-0007.jpg
FDP-0012.png
```

Supported formats are `.jpg`, `.jpeg`, `.png`, and `.bmp`. Review Meal uses
the image in recipe view, print preview, printing, and printable HTML export.
